<!--
⚠️ IMPORTANT: READ THIS BEFORE CREATING YOUR PULL REQUEST

To enable automatic PR templating, follow the instructions for your target branch:

--------------------------------------------------------------------------------
1. TARGET BRANCH IS "stage" (Basic Feature or Fix PR):
   • STEP 1: Enter a descriptive title starting with your Conventional Commit prefix (e.g., "orca-feat: add workspace labels").
   • STEP 2: Do NOT write or edit anything in this description box.
   • STEP 3: Click "Create pull request" at the bottom.
   • STEP 4: Wait 5 seconds. The GitHub Action will automatically load the Basic PR template. Click "Edit" on the PR description to fill out the checklist.

--------------------------------------------------------------------------------
2. TARGET BRANCH IS "prod" (Release Candidate Promotion PR):
   • STEP 1: Leave the PR title as default (the Action will automatically set it to the correct version name: "orca-release: Promote Release Candidate v1.3.X...").
   • STEP 2: Do NOT write or edit anything in this description box.
   • STEP 3: Click "Create pull request" at the bottom.
   • STEP 4: Wait 5 seconds. The GitHub Action will automatically load the Release Candidate checklist. Click "Edit" on the PR description to verify and check off the QA items.
--------------------------------------------------------------------------------
-->
